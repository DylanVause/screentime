services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: ${SECRET_KEY}
      DB_PATH: /data/screentime.db
    volumes:
      - screentime_data:/data
    restart: unless-stopped

volumes:
  screentime_data:
